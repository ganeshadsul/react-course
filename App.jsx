import React from 'react';
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementById('root'));
const heading = <h1>This is h1 tag inside jsx</h1>;
console.log(heading);

root.render(heading);
